<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hub_delivery_view_form" model="ir.ui.view">
        <field name="name">hub.delivery.form</field>
        <field name="model">hub.delivery</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                      <!-- <field name="address"/> -->
                      <field name="state_group_id"/>

                      <!-- <field name="state_id" widget="many2many_tags"/> -->
                      <field name="delivery"/>
                    </group>
                </group>
                <!-- <div role="alert" class="alert alert-warning" attrs="{'invisible': [('invoicing_message', '=', '')]}">
                    <field name="invoicing_message" nolabel="1"/>
                </div>
                <div role="alert" class="alert alert-info" attrs="{'invisible': [('delivery_message', '=', False)]}">
                    <field name="delivery_message" nolabel="1"/>
                </div> -->
                <footer>
                    <!-- <button name="button_confirm" type="object" string="Add" class="btn-primary" data-hotkey="q"/> -->
                    <button string="Discard" special="cancel" data-hotkey="z" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
